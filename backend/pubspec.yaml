name: skycast_backend
description: Backend API for SkyCast app
version: 1.0.0
environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  http: ^1.2.1
  dotenv: ^4.2.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.2
