services:
  backend:
    build:
      context: ./backend
    container_name: skycast-backend
    env_file:
      - ./backend/.env
    environment:
      HOST: 0.0.0.0
      PORT: 8081
    ports:
      - "8081:8081"
    restart: unless-stopped
